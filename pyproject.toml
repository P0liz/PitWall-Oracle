[project]
name = "pitwall-oracle"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pdfplumber>=0.11.10",
]


[tool.black]
skip-magic-trailing-comma = true
line-length = 120  # or whatever works for you
